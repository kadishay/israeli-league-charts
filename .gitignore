# Cached source pages and per-source parse output. Both are build artefacts:
# the fetch scripts regenerate the pages, the parse scripts the CSVs, and
# data/seasons.csv is the one dataset that is committed.
data/raw/

# Generated charts are committed: they are the point of the repo, and
# out/index.html needs the sibling SVGs to resolve. The two gallery-body files
# are fragments for publishing only.
out/gallery-body.*.html

__pycache__/
*.pyc
.DS_Store
